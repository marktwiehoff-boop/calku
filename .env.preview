# Nur für lokale Browser-Verifikation (vite --mode preview): Cloud-Modus aus,
# App läuft im lokalen Modus ohne Login. Hat keinen Einfluss auf Build/Deploy.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
